spring.application.name=SmartFarm

server.port=8081

# ===============================
# 🗃️ Configuration PostgreSQL
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/smartfarm_db
spring.datasource.username=postgres
spring.datasource.password=fraj

# Driver PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# 🧱 JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# 🕊️ Flyway (migration base de données)
# ===============================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# ===============================
# 🧰 DevTools
# ===============================
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# JWT Secret
jwt.secret=Z3Vuc2NoM1VzaVh4dGhHZmZVb2VwV3hpYWdZTnV3b3Q=
jwt.expiration=86400000

# Error Debugging
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
